# The top-down human answer that the bottom-up classification rollup has to reproduce.
# If a data edge changes such that a system's tier would move, that is either a real
# finding or a data-entry mistake, and both should stop the build rather than silently
# reclassify a system.
#
# Generated by scripts/export-facts-to-yaml.mjs. Edit this file, not a TypeScript module.

SYS-003: "Restricted"
# Was "Confidential" until Okta (AST-003-23) became a shared asset spanning
# both systems: Okta accesses DT-004 (Encryption Keys & Secrets, Restricted)
# on SYS-003, and that access is now legitimately Workday's too — a real
# consequence of shared identity infrastructure, not a data-entry mistake.
SYS-042: "Restricted"
