# Controls declared absent. Knowing a control is absent IS a measurement — it is the one
# case where a zero is a finding rather than a blank.
#
# Generated by scripts/export-facts-to-yaml.mjs. Edit this file, not a TypeScript module.

- assetId: "AST-042-03"
  controlId: "MON-03"
  reason: "The tenant emits no per-action log for this service account's API calls, so there is
    nothing to shape into a conformant event. Raised with the vendor; no ACME-side implementation is
    possible today."
- assetId: "AST-042-05"
  controlId: "NET-17"
  reason: "Content inspection on the identity feed has not been built. The feed carries employee
    identifiers by design, so the control applies and is simply outstanding."
- assetId: "AST-042-02"
  controlId: "NET-17"
  reason: "Inspection on the tenant API surface is not deployed; the DLP work to date has been scoped
    to the export path only."
