# Asset/control pairs a rule would have required, excused with a stated reason. An
# exception without a reason is indistinguishable from nobody having looked, so the
# reason is required.
#
# Generated by scripts/export-facts-to-yaml.mjs. Edit this file, not a TypeScript module.

# An exception means the control's premise does not hold here and nobody implements it.
# That is a different claim from "required, but operated by the vendor" — which is now a
# responsibility on the implementation (see implementation-mechanisms.yaml) rather than an
# excusal. AST-003-07/CFG-02 moved for exactly that reason: telling an auditor the KMS key
# doesn't need a secure baseline was false, because it does and AWS provides it.

- assetId: "AST-042-07"
  controlId: "DCH-18"
  reason: "Retention for the exported log stream is set and enforced in ACME's central monitoring
    destination, not in the vendor feed. Assessing it here would double-count the same control the
    destination already carries."
